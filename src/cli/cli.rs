use configuration::Settings;

fn main() -> Result<(), serde::yaml::Error> {

    // Insert code for variable holding default values

    // Insert code for serializing variable
    // e.g. let s = serde_yaml::to_string(&<variable_name>);

}